import type { Editor } from "grapesjs";
import { commonTraits } from "../configs/traits"; 
import { UlComponent } from "./Tag/UlComponent";
import { TDComponent } from "./Tag/TdComponent";

export const PluginsTagComponents = (editor: Editor) => {
//   UlComponent(editor, commonTraits); 
//TDComponent(editor, commonTraits); 
};
